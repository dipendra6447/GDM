import Page from '@/views/SubscriptionLight/SubscriptionLight';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'JobNest Premium — Subscription & Pricing Plans',
  description: 'Unlock unlimited job applications, employer recruitment tools, and business promotion features with flexible pricing plans.',
};

export default function SubscriptionLightPage() {
  return <Page />;
}
